server:
  port: 8761

application:
  name: Eureka Server
  version: SNAPSHOT-1.0
spring-boot:
  version: 3.2.4

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true

  client:
    register-with-eureka: false
    fetch-registry: false
